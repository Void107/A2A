# A2A Contract Hub 工作入口

本仓库是当前主实现。`../A2A/a2a-contract-hub/` 是历史副本，不要同步改写或用它替换主实现。

## 阅读顺序

处理产品改造、OPA/Presidio 集成或“按照计划执行”请求时，先读：

1. `docs/implementation/README.md`
2. `docs/implementation/PRODUCT.md`
3. `docs/implementation/CONTRACT_SPEC.md` 及 `examples/`
4. `docs/implementation/EXECUTION_PLAN.md`
5. `docs/implementation/ACCEPTANCE.md`
6. `docs/implementation/TASKS.json` 和 `STATUS.md`

这些文件是目标设计，不是当前能力说明。当前用户指令优先；不要因为读取本文件，就自动执行与当前请求无关的整套改造。

## 执行约定

- 用户要求执行改造时，按 TASKS.json 的依赖顺序完成可执行任务；每个任务做到实现、必要测试和证据记录闭环，不以“已写计划”代替实现。
- 开始前查看 Git 状态；保留用户已有改动，不重置、覆盖或删除历史目录、数据库、密钥和现有虚拟环境。
- 只写文档的请求只更新文档与合成样例，不启动业务实现。阅读文档不代表业务任务完成。
- 常规实现选择自行判断，在 STATUS.md 记录。不要为已经明确的范围反复请求确认；缺失真实凭据、外部参与者或权限时，记录具体阻碍并继续独立任务。
- 本地实现与验证不等同于授权对外部署、推送仓库、联系人员或操作真实生产数据。遵守当前会话的授权和执行环境权限。
- 默认使用合成数据。不要把 `.env`、令牌、原始敏感响应写进文档、普通日志、测试快照或提交。
- 将现有测试、增加的测试和真正运行结果分别记录。环境加载失败不是业务测试通过；不得通过删测试、放宽预期或返回原文来制造通过。
- 只有一个授权结果决定放行；OPA 不可用、决策缺失或格式错误时不得回退到旧策略放行。
- 必需处理未完成、输出结构无效或交付记录无法持久化时不得返回数据。已撤销权限不能由旧契约、缓存或回滚恢复。
- 探索和测试目标代码必须走真实业务入口；不要为测试维护一套行为不同的简化执行引擎。
- 可以并行处理互不写同一文件的任务；负责人必须做最终接口和验收一致性检查。
- 不假定旧 README 中的安全、并发和规范兼容声明已成立。新设计的行为以 CONTRACT_SPEC.md 为准；任务状态以 TASKS.json 为准。

## 交付

完成任务时更新 TASKS.json 对应状态和证据，追加 STATUS.md 的变更、验证结果、尚未验证项及下一任务。外部用户试用未发生时如实保持待验证，不宣称成熟生产版本。
