# T00/T01 execution evidence — 2026-09-13

Base HEAD: `2015380`. The pre-existing README, product document, AGENTS.md and
implementation documents were uncommitted before this work and were preserved.
No production database, old venv, historical repository or credentials were modified.

## T00 — partially implemented, BLOCKED on real test infrastructure

- Host: macOS arm64; available system Python 3.9.6. Created a new `.venv` (the
  recommended Python 3.12 is not installed). New wheels import and tests execute
  normally; no system security settings or native-extension checks were disabled.
- Network installation failed inside the sandbox (DNS); the approved escalation
  installed the pinned requirements. `requirements-test.txt` lists test additions;
  `requirements-test.lock` records the entire resolved environment. This lock is
  evidence for Python 3.9/macOS, not proof of Python 3.12 compatibility.
- Prior default naming collects **39 tests**. `pytest.ini` adds `hacker_agent.py`,
  making all **49 existing tests** discoverable. The existing suite passed:
  `t00-baseline.log`, 49 passed, 2 weak synthetic signing-key warnings.
- `tests/test_security_regressions.py` invokes actual legacy implementation
  functions. **8 failed / 1 passed** before security fixes. No xfail, skip or
  relaxed assertion was introduced. Failures expose unknown effect, missing
  transform, ignored condition, unsupported custom/avg, invalid truncate, wildcard
  no-op and swallowed audit persistence failure. Default-deny positive control passes.
- These tests establish defects; their successful reproduction is **not** a
  security acceptance pass. Existing tests include a separate legacy QA transform
  helper and SQLite/FakeRedis; their green result does not prove the new pipeline.
- `compose.test.yaml` prepares isolated PG 17.6 and Redis 7.4.5 instances on
  loopback ports 55432 and 56379, with synthetic local test credentials and a
  dedicated test volume. `scripts/check_test_services.py` checks only those exact
  instances and never reads the project's `.env`. Configuration is **NOT_RUN**:
  Docker/PG/Redis executables were not found. No existing service was probed.
- Required external input: an available local container runtime or dedicated test
  PG/Redis configuration. Start with `docker compose -f compose.test.yaml up -d
  --wait`, then `.venv/bin/python scripts/check_test_services.py`. Confirm isolation
  before applying migrations. Image acquisition, service readiness, migrations,
  lifespan and restart behavior remain unverified; T00 cannot be marked done.

## T01 — completed within the static-validation task scope

Implementation: `app/contracts/contract.schema.json`, `loader.py`, `decision.py`,
CLI `python -m app.contracts`, generator `scripts/build_contract_schema.py`,
`policies/contract.rego`, and associated Python/Rego tests.

- Closed recursive meta-schema plus semantic validation: no unknown fields or
  vocabulary, coercion, remote schema resolution, duplicate IDs/JSON keys, invalid
  references, object-wide projection, unsupported processor/language/entity or
  unreviewed patterns. Source and output have explicit closed objects. Paths
  resolve to leaves, array pairing is represented by `[*]`, output path sets and
  types match projection. Runtime schema validation executes date/email formats.
- Bounded input size/depth; finite numbers; detached copies and canonical SHA256.
  CLI validates offline and does not activate, publish or grant permission.
- Conservative supported subset: object arrays; date/email formats; the two
  reviewed meeting/item identifier patterns; stable three-component contract
  versions. Additional schema capabilities require explicit implementation and
  tests. The loader's output is mutable JSON: the future registry must persist an
  immutable snapshot; this is not yet a registry/version guarantee.
- Fixed Rego implements within-dimension OR, cross-dimension AND, deny priority,
  current active grant/resource intersection and fixed required processors. No
  new Python allow evaluator or legacy fallback was introduced. Runtime OPA HTTP
  integration and trusted identity construction remain T06/T02.
- Actual OPA binary: **1.0.0**, darwin/arm64 static, build commit
  `00cc7ae2757b456f60e211ea55319175ae669556`, Rego v1, no WASM. Official binary
  SHA256 verified against release asset:
  `836cfeaa6713c59ae681e4f1e2f510980ae3df15112adc30dc6cebc5ff636db4`.
  Downloaded to `/private/tmp/a2a-opa-1.0.0`; not vendored or installed system-wide.
  An initial incorrect non-static filename returned 404; GitHub API lookup
  returned 403; the official expanded asset listing resolved the correct filename.
- Sources checked: [OPA policy language](https://www.openpolicyagent.org/docs/policy-language)
  and [OPA 1.0.0 assets](https://github.com/open-policy-agent/opa/releases/expanded_assets/v1.0.0).
  This version establishes semantics only, not a current production dependency
  recommendation or a security-reviewed release choice.

Reproduce from repository root:

```sh
.venv/bin/python -m pip install -r requirements-test.lock
.venv/bin/python -m pytest tests/contracts -q
.venv/bin/python -m app.contracts docs/implementation/examples/contract.meeting.json
/private/tmp/a2a-opa-1.0.0 test policies -v
.venv/bin/python -m pytest -q
```

Results: **48 Python contract tests passed**, **7/7 real OPA policy tests passed**,
CLI accepted the golden contract. Full suite: **98 passed / 8 failed**, exit 1;
those 8 are the unfixed security regressions above. Exact commands and exit codes:
`run-results.json`; logs: `t01-tests.log`, `t01-cli.log`, `t01-opa.log`,
`current-suite.log`. `git diff --check` passed.

AC-01 remains NOT_RUN as an end-to-end acceptance: server activation, immutable
storage and forged lifecycle rejection are not implemented. AC-05/06 likewise
are not marked PASS from the policy-only tests. No alpha or readiness claim.

## Next work and external evidence

Resolve T00's isolated real services, then T02 trusted identity/scope/grants, T03
signed approved upstream, and T04 transactional receipt/outbox. Preserve the
failing regression evidence and repair the actual production paths. No new
v2 delivery route is exposed yet; existing legacy routes remain legacy and must
not be treated as a secure data-contract deployment.

Presidio, dual-view end-to-end consumption, A2A official SDK interoperation,
version lifecycle/change checks, real faults/performance, clean startup and the
alpha package are NOT_RUN. T15 has a preparation template only; a human
participant and authorization to arrange a trial must come from the user.
